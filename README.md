**Visionary URL** is a lightweight library for generating image URLs with baked-in blurhash placeholder data.


## Getting started

Install `visionary-url` via npm. Both ES modules and CommonJS modules are supported.

```bash
npm install --save visionary-url
```

